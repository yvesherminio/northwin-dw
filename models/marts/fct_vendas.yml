version: 2
models:
  - name: fct_vendas
    description: Dimensão de produtos da Northwind.
    columns:
      - name: pk_vendas
        description: Chave primária da tabela produtos.
        tests:
          - unique
          - not_null